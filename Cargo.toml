[workspace]
resolver = "3"
members = ["toa", "toa-blob", "toa-cli", "toa-fuse", "toa-hash", "fuzz", "toa-blob-compress", "toa-blob-store"]

[profile.dev]
opt-level = 1

[profile.release]
opt-level = 3
lto = "fat"
