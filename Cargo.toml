[workspace]
resolver = "3"
members = ["toa", "toa-blob", "toa-cli", "toa-fuse", "toa-hash", "toa-unix", "fuzz", "toa-blob-compress"]

[profile.dev]
opt-level = 1

[profile.release]
opt-level = 3
lto = "fat"
