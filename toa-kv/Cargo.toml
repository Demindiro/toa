[package]
name = "toa-kv"
version = "0.1.0"
edition = "2024"

[dependencies]
sled = { version = "0.34.7", default-features = false, optional = true }

[features]
alloc = []
sled = ["dep:sled", "std"]
std = ["alloc"]
