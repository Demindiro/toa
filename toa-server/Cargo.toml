[package]
name = "toa-server"
version.workspace = true
edition.workspace = true

[features]
trace = []

[dependencies]
popol = "3.0.1"
toa = { path = "../toa", features = ["lz4", "zstd"] }
toa-blob = { path = "../toa-blob", features = ["std"] }
toa-hash = { path = "../toa-hash" }
