[package]
name = "appender"
version = "0.1.0"
edition = "2024"

[features]
std = ["alloc"]
alloc = []

[dependencies]
blake3 = { version = "1.8.2", default-features = false }
chacha20poly1305 = { version = "0.10.1", features = ["reduced-round"] }

[dev-dependencies]
rand = { version = "0.8.0" }
