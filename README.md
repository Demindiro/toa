# Appender

Appender is an append-only object-/filesystem for immutable storage.

## Building

The **easiest, most portable** method of building this software is to simply run `./waddle/build.sh`.
It requires Linux user namespaces to be enabled however.

## Reasoning

TODO (the previous one was bad and rambly).


